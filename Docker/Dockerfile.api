FROM node:20-alpine

WORKDIR /app

# Install deps with npm only
COPY package.json package-lock.json ./
RUN npm install

# Copy app source
COPY . .

# Build TypeScript
RUN npm run build

EXPOSE 3000

# Run compiled JS
CMD ["node", "dist/index.js"]
